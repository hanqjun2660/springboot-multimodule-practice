tasks.named('bootJar'){ // bootJar 세팅을 켜기
    enabled = false
}

tasks.named('jar'){ // jar 설정은 끄기
    enabled = true
}

dependencies {  // 하위 모듈 추가
    implementation project(':internalAPI:user')
}

subprojects {
    dependencies {
        implementation project(':core')
    }
}
